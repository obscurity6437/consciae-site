# Never run dependency lifecycle scripts. The only install script in the tree
# is optional fsevents (chokidar falls back to polling without it). `npm run`
# scripts in this package are unaffected.
ignore-scripts=true
